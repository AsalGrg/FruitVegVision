# Backend dependencies
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
opencv-python>=4.9.0
numpy>=1.26.0
pillow>=10.2.0

# Optional: only needed when you have a real trained .pt model to serve.
# If missing, the server falls back to demo mode automatically.
ultralytics>=8.2.0
